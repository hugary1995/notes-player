__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/
.DS_Store

# Local MCP registration written by `claude mcp add -s project` during dev.
# Not shared — students install via `uvx --from git+...` per the README.
.mcp.json
